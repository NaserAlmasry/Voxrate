PORT=3001

# ── Proxy config (pick ONE of the two options below) ──────────

# Option A: Direct proxy URL — Webshare, Oxylabs, or any HTTP proxy
# Format: http://username:password@host:port
PROXY_URL=http://vhclvkvf:c3ysv84uanao@38.154.203.95:5863

# Option B: BrightData residential (session-pinned, better for production)
# Get these from BrightData > Proxies > Your Zone > Access parameters
# Comment out PROXY_URL above and fill these in when switching to BrightData
# BRIGHTDATA_CUSTOMER_ID=your_customer_id
# BRIGHTDATA_ZONE=voxrate_scraper
# BRIGHTDATA_PASSWORD=your_zone_password

# ── Auth ──────────────────────────────────────────────────────
# Shared secret — Vercel sends this in X-Scraper-Secret header
# Generate with: openssl rand -hex 32
SCRAPER_SECRET=your_random_secret_here
